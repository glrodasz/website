# Production origin (no trailing slash). Canonical URLs, Open Graph, and Twitter cards.
VITE_SITE_URL=https://guillermorodas.com

# Brevo (AI-First waitlist) — server-only, NOT prefixed with VITE_ so they stay
# out of the client bundle. Set these in the Vercel project settings for
# Production and Preview environments.
BREVO_API_KEY=
BREVO_AI_COURSE_LIST_ID=
